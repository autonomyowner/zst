{
  "name": "zst-marketplace",
  "version": "1.0.0",
  "description": "Multi-tier B2B/B2C e-commerce platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:client": "npm --workspace apps/client run dev",
    "dev:server": "npm --workspace apps/server run dev",
    "dev": "concurrently -n client,server -c blue,green \"npm:dev:client\" \"npm:dev:server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
